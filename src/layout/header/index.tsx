import React from 'react';
import { PageHeader } from 'antd';

import './styles.less';

const Header = () => {
  return <PageHeader style={{ width: '50%', marginLeft: '45%' }} title="Copy Data to Folder" />;
};

export default Header;
